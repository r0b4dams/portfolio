name: Vars

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - main

jobs:
  vars:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: View contexts
        run: echo ${{ github }}
